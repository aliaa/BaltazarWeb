@model IEnumerable<BaltazarWeb.Models.ShopItem>

@{
    ViewData["Title"] = "لیست فروشگاه";
}

<h2>لیست فروشگاه</h2>

<p>
    <a asp-action="Add">افزودن محصول جدید</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => model.Enabled)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CoinCost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateAdded)
            </th>
            <th>
                تصویر
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink("ویرایش", "Edit", new { id = item.Id.ToString() }) |
                @Html.ActionLink("جزئیات", "Details", new { id = item.Id.ToString() }) |
                @Html.ActionLink("حذف", "Delete", new { id = item.Id.ToString() }, new { onclick = "return confirm('آیا برای حذف مطمئن هستید؟');" })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Enabled)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoinCost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @(Utils.GetPersianDateString(item.DateAdded))
            </td>
            <td>
                @if (item.HasImage)
                {
                    <a href="@(Consts.UPLOAD_IMAGE_DIR.Replace("\\","/") + "/" + item.Id + ".jpg")">نمایش تصویر</a>
                }
            </td>
        </tr>
}
    </tbody>
</table>
